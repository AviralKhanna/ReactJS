/* CUSTOM HOOKS
why? -> when we need to fetch API's we need to have custom hooks to handle them.
custom hooks help us return the desired data from the API we have called.

*/

import { useState } from "react";
import "./App.css";

function App() {
  const [count, setCount] = useState(0);

  return <></>;
}

export default App;
